# include <stdio.h>

int main(void)
{

	return 0;
}
/*
continue  用于跳过本次循环余下的语句,转去判断十分需要执行下次循环.

for(1; 2; 3)
{
    A;
	B;
	continue;//如果执行该语句,则执行完该语句后,会执行语句3, C 和 D都会被跳过去,C和D都不会被执行.
	C;
	D;
}

while (表达式)
{
    A;
	B;
	continue;//如果执行该语句,则执行完该语句后,会执行表达式,C 和 D都会被跳过去,C和D都不会被执行.
	C;
	D; 
}
*/