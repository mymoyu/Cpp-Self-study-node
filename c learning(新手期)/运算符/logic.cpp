# include <stdio.h>

int main(void)
{
	int i = 10;
	int k = 20;
	int m;

//  m = (3>2) && (k = 8);//把8赋予了k; 结果:m = 1,k = 8
//	m = (3>2) && (k = 0);//结果为m = 0,k = 0
//	m = (1>2) && (k = 5);//结果为m = 0,k = 20;   不含有分号的是表达式,含有分号的是语句
    m = (1>2) || (k = 5);//结果为m = 1,k = 5;
	printf("m = %d, k = %d\n", m, k);

	return 0;
}
/*  
    C语言对真假的处理
    非0即真,0即假

  &&左边的表达式为假   右边的表达式肯定不会执行
  ||左边的表达式为真   右边的表达式肯定不会执行
*/