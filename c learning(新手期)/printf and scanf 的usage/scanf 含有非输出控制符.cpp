/* 
scanf用法二:  sacanf("非输入控制符 输入控制符", 参数);
     功能:    将从键盘中输入的字符转化为输入控制符所规定格式的数据,
	          然后存入以输入参数的值为地址的变量中
			  非输入控制符必须原样输入(否则会出现错误)
*/
# include <stdio.h>

int main(void)
{
	int i;

	scanf("m%d", &i);//输入123 输出错误(非法输入);输入m123 输出 i = 123(正确输入)
	printf("i = %d\n", i);

	return 0;
}